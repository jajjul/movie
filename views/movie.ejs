<!DOCTYPE html>
<html>

<head>
  <title>검색</title>
  <link rel='stylesheet' href='/stylesheets/movie.css' />
</head>

<body>
  <table>
    <% for(var i=0; i < list.length; i++) { %>
    <tr>
      <td><%= list[i].id %></td>
      <td><%= list[i].name %></td>
    </tr>
    <% } %>
  </table>
</body>

</html>